import pygame;

def __main__():
    pygame.init();
    pygame.display.init();
    screen = pygame.surface()
    pygame.display.set_mode((400,400), flags=0, display=0);
    pygame.draw.rect(screen,(30,30), (255,255,0));
while(True):
    __main__();
